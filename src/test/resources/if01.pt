from java.lang.Math import cos as fun;
def cycle(start, step) {
	fun(rep(x <- start) {x + step})
}
def switch(v) {
	if(cycle(-pi*2/3, pi/3) >= 0) {
		rep(x <- 0) {
			x + 1
		}
	} else {
		rep(x <- 0) {
			x - 1
		}
	}
}
switch(0)