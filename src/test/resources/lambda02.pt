from java.lang.Math import cos;
def cycle(start, step) {
	cos(rep(x <- start) {x + step})
}
def switch() {
	if(cycle(-pi*2/3, pi/3) >= 0) {
		() -> {
			rep(x <- 0) {
				x + 1
			}
		}
	} else {
		() -> {
			rep(x <- 0) {
				x - 1
			}
		}
	}
}
((switch).apply()).apply();