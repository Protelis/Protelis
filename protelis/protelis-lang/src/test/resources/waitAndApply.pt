// EXPECTED_RESULT: true
import protelis:state:time
import protelis:state:nonselfstabilizing:time
waitAndApply(0, () -> {true}, false) && 
	if(timeSinceStart() < 2) { !waitAndApply(2, () -> {true}, false) } 
	else { waitAndApply(2, () -> {true}, false) }
