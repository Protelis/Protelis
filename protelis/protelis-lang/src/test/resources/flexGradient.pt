import protelis:coord:spreading
import protelis:lang:utils
import protelis:state:nonselfstabilizing:time
let source = env.has("source") || getUID() == "5" && timeSinceStart() <= 50;
flexGradient(source, 0.1, 3, 1.5, 0.1)