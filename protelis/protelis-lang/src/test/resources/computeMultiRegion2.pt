import protelis:coord:meta
import protelis:lang:utils
import protelis:coord:nonselfstab:accumulation

computeMultiRegion(
	env.get("childPartition"), 
	(key) -> { key < 0.6 },
	() -> { gossip(env.get("n"), min) },
	Infinity
)
