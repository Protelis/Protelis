import protelis:coord:spreading
import protelis:lang:utils
import java.lang.Double.POSITIVE_INFINITY

let res = gradient(env.has("source"), 0, nbrRange, identity);
if (res != POSITIVE_INFINITY) { res.get(0) + res.get(1) } else { "foo" }