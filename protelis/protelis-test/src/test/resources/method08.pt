// EXPECTED_RESULT: "14"

import java.lang.String.format
import java.util.Arrays.asList

// Check call of over-length and minimum-length varargs method
let l = asList(1,2,3,4,5); // Check call of varargs with multiple args
l.addAll(asList()); // Check call of varargs with zero args
// Check call of dynamically selected varargs method 
l.get(3)// + format("Stringy %s %s", "a", "b").length();
